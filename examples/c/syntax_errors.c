#include <stdio.h>

int main(void) {
    int value = 10
    printf("value = %d\n", value);

    for (int i = 0 i < 3; i++) {
        printf("%d\n", i);
    }

    if (value > 5 {
        printf("large\n");
    }

    return 0;

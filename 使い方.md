# 構文エラー解析ツール 使い方

このたびは「構文エラー解析ツール」をご利用いただき、ありがとうございます。

本ツールは、プログラミング初心者がC、Java、Python、JSONの構文エラーについて、問題が起きた場所、原因になった可能性が高い場所、修正候補を日本語で確認するためのデスクトップツールです。

![構文エラー解析ツールの画面](image/01_demo.png)

画面左側にソースコード、右側に解析結果と修正候補が表示されます。左右の境界をドラッグすると、それぞれの表示幅を変更できます。

## 1. 動作環境

- Windows、macOS、Linux
- Python 3.10以降
- Tkinter
- インターネット接続（初回の依存パッケージ導入時のみ）

本ツールは入力したソースコードを実行しません。構文と周辺の文字だけを解析します。

## 2. ダウンロードしたファイルを展開する

BOOTHからダウンロードしたZIPファイルを右クリックし、「すべて展開」を選択します。

展開後、`main.py`、`requirements.txt`、`syntax_helper`フォルダーが同じフォルダー内にあることを確認してください。ZIPファイルの中から直接起動せず、必ず展開してから使用してください。

## 3. Pythonを準備する

Python 3.10以降がインストールされているか確認します。PowerShellまたはターミナルを開き、次を実行してください。

```powershell
python --version
```

`Python 3.10`以上のバージョンが表示されれば使用できます。`python`が見つからない場合は、Python公式サイトからPythonをインストールしてください。

WindowsでPythonをインストールするときは、「Add python.exe to PATH」を有効にすることをおすすめします。Tkinterも一緒にインストールしてください。

## 4. 初回セットアップ

PowerShellまたはターミナルで、展開したツールのフォルダーへ移動します。

例：

```powershell
cd "C:\Users\ユーザー名\Downloads\syntax_error_helper"
```

続けて、ドラッグ＆ドロップ機能に必要なパッケージをインストールします。

```powershell
python -m pip install -r requirements.txt
```

この操作は通常、最初の1回だけ必要です。

## 5. ツールを起動する

ツールのフォルダーで次を実行します。

```powershell
python main.py
```

「構文エラー解析ツール」という画面が表示されたら起動完了です。

## 6. コードを解析する

### 直接入力する場合

1. 画面上部の「言語」からC、Java、Python、JSONのいずれかを選択します。
2. 「ソースコード」欄へコードを入力または貼り付けます。
3. 「解析」ボタンを押します。
4. 画面右側の「解析結果・修正候補」を確認します。

### ファイルを読み込む場合

解析したいファイルを、エクスプローラーやFinderから「ソースコード」欄へドラッグ＆ドロップします。

対応する拡張子は次のとおりです。

| 言語 | 対応拡張子 |
| --- | --- |
| C | `.c`、`.h` |
| Java | `.java` |
| Python | `.py` |
| JSON | `.json` |

ファイルをドロップすると、拡張子に対応する言語が自動的に選択されます。

- UTF-8とCP932のファイルを読み込めます。
- 読み込めるファイルサイズは2 MB以下です。
- 複数ファイルを同時にドロップした場合は、最初の1ファイルを読み込みます。

## 7. 解析結果の見方

### 左側のソースコード

- 薄い赤系の行：構文解析で問題が見つかった行、または実際の原因である可能性が高い行です。
- 行番号：解析結果に表示された問題位置や原因候補位置と対応しています。

### 右側の解析結果・修正候補

- 薄い赤色の部分：問題が見つかった位置、原因候補の位置、問題の内容、理由を表示します。
- 薄い緑色の部分：追加や書き換えを検討できる修正候補を表示します。
- 区切り線：複数のエラー候補がある場合に、候補同士を分けて表示します。

例えば、前の行にセミコロンがないため次の行でエラーになった場合は、次の行だけでなく、セミコロンが不足している可能性がある前の行も原因候補として表示します。

複数の間違いが疑われる場合は、複数の候補が表示されます。上に表示された候補から順番に修正し、もう一度「解析」を押してください。

## 8. 検証用サンプルを試す

`examples`フォルダーには、各言語の正常なコードと、意図的に構文エラーを含めたコードがあります。

- `valid`を含むファイル：正常コードの確認用
- `syntax_errors`を含むファイル：エラー検出の確認用

ファイルをエディタへドラッグ＆ドロップすると、すぐに動作を確認できます。

## 9. よくある問題

### `python`が見つからない

Pythonがインストールされていないか、PATHが設定されていない可能性があります。Windowsでは、次のコマンドもお試しください。

```powershell
py --version
py -m pip install -r requirements.txt
py main.py
```

### `No module named 'tkinterdnd2'`と表示される

ツールのフォルダーで次を実行してください。

```powershell
python -m pip install -r requirements.txt
```

### Tkinterに関するエラーが表示される

次のコマンドでTkinterを確認できます。

```powershell
python -m tkinter
```

小さな画面が表示されれば正常です。表示されない場合は、PythonのインストーラーからTkinterを追加してください。

### ファイルをドロップできない

- 初回セットアップが完了しているか確認してください。
- 対応する拡張子か確認してください。
- ファイルが2 MB以下か確認してください。
- ツールとエクスプローラーを異なる権限で起動している場合は、同じ権限で起動してください。

## 10. ご利用上の注意

- 原因位置と修正候補は推定であり、100%正しいとは限りません。
- 本ツールはコードの実行結果、型エラー、論理エラー、すべてのコンパイルエラーを保証するものではありません。
- 大切なソースコードは、修正前にバックアップしてください。
- 本ツールの結果だけで判断せず、必要に応じて授業資料、公式ドキュメント、コンパイラのメッセージも確認してください。
